# 可选：需要云端多设备同步时，在 Supabase 控制台 Project Settings → API 复制以下两项。
# 若本文件不配置或删除这两行，应用将只使用浏览器本地数据库（IndexedDB / Dexie），无需登录。

# VITE_SUPABASE_URL=https://你的项目.supabase.co
# VITE_SUPABASE_ANON_KEY=你的_anon_public_key
